Create a filter modal that can be a standard component across all the pages that allows a user to filter the content of the list component based on critira they select in the modal. Add the modal to all the pages with a filter button. Any filter buttons that don't align with the others should be update to match. 